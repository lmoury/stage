<nav class="navbar navbar-expand-lg navbar-light mb-4" role="navigation">
    <div class="container-fluid">
        <a class="mr-4" href="{{ path('/') }}"/><img src="/assets/img/dachser_logo.svg"/></a>
        <button class="navbar-toggler border-0" type="button" data-toggle="collapse" data-target="#exCollapsingNavbar">
            &#9776;
        </button>
        <div class="collapse navbar-collapse" id="exCollapsingNavbar">
            <ul class="nav navbar-nav">
                <li class="nav-item {% if current_url is defined and current_url == 'quais' %}active{% endif %}"><a href="{{ path('quai') }}" class="nav-link">Quais</a></li>
                <li class="nav-item {% if current_url is defined and current_url == 'parking' %}active{% endif %}"><a href="{{ path('parking') }}" class="nav-link">Parking</a></li>
                <li class="nav-item {% if current_url is defined and current_url == 'planning' %}active{% endif %}"><a href="{{ path('planning') }}" class="nav-link">Planning</a></li>
                <li class="nav-item {% if current_url is defined and current_url == 'traction' %}active{% endif %}"><a href="{{ path('traction') }}" class="nav-link">Tractions</a></li>
                <li class="nav-item {% if current_url is defined and current_url == 'remorques' %}active{% endif %}"><a href="{{ path('remorques') }}" class="nav-link">Remorques</a></li>
            </ul>
            <ul class="nav navbar-nav flex-row justify-content-between ml-auto">
                {% if app.user %}
                    {% if is_granted('ROLE_EMPLOYE') %}
                    <li class="nav-item order-2 order-md-1"><a href="{{ path('admin') }}" class="nav-link" title="settings">Panneau d'admin</a></li>
                    {% endif %}
                <li class="order-1">
                    <a class="btn btn-outline-secondary" href="{{ path('logout') }}"><i class="fa fa-power-off"></i></a>
                </li>
                {% else %}
                <li class="order-1">
                    <a href="{{ path('login') }}" class="btn btn-outline-secondary">Login</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
