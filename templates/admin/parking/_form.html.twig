{{ form_start(form) }}
<div class="row">
    <div class="col-md-7">
        {{ form_widget(form.denomination) }}
        {{ form_errors(form.denomination) }}
    </div>
    <div class="col-md-5">
        {{ form_rest(form) }}
        <div class="float-right btn-container">
            <button class="btn btn-primary">{{ button|default('Modifier')}}</button>
        </div>
    </div>
</div>
{{ form_end(form) }}
